---
description: 共享包 @llcz/common 发布与导出约定
globs: packages/common/**/*
alwaysApply: false
---

# @llcz/common

## 角色

- 纯 TS **工具库**，发布为 dual package（`main`/`module`/`types` 指向 `dist/`）。
- 消费者：**admin、server**（workspace:*）；**uni 尚未接入**。
- **禁止**引入 Vue、Nest、DOM 或任意 UI 框架。

## 当前导出

从 `packages/common/src/index.ts` 统一导出：

- `is` — 类型判断（如 `isArray`）
- `dateUtil` — 日期工具
- `tree` — 树形数据处理
- `uuid` — UUID 生成
- `qs` — querystring
- `format` — 格式化

## 开发流程

- 源码在 `packages/common/src/`；新工具在子文件实现并在 `index.ts` 导出。
- 构建：`pnpm --filter @llcz/common build`（Rollup）；改代码后各 app workspace 联调。
- 保持函数纯、可 tree-shake；类型完整，避免 `any`。
