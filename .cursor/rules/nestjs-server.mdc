---
description: NestJS 后端（@apps/server）模块、鉴权与接口约定
globs: apps/server/**/*.{ts,yml,yaml}
alwaysApply: false
---

# Server（NestJS）

## 结构

- 业务按 **Nest 模块** 划分：`src/modules/<domain>/`（controller、service、dto、entities 等）。
- 模块域：`system/*`（用户/角色/菜单/部门等）、`monitor/*`（日志/缓存/服务器）、`upload`、`tasks`、`area` 等；新功能按域建 module。
- 全局能力在 `src/common/`（守卫、过滤器、装饰器、工具）、`src/plugins/`、`src/config/`。
- **通用能力放 `src/plugins/`**：OCR（`OcrService`）、LLM（`OpenAIService`）、HTTP/MQTT/邮件等；通过 `PluginsModule` 导出，业务模块 `imports: [PluginsModule]` 注入使用。
- **业务模块只保留领域逻辑**：prompt、DTO/校验、编排流程、错误码到用户文案的映射；不要把 RapidOCR 初始化、chat/completions 请求等通用实现写进 `modules/`。
- 数据库：**TypeORM**；迁移在 `src/migrations/`，通过 `migration:*` 脚本执行。
- 配置：`src/config/dev.yml` / `prod.yml` + `typeorm.config.ts`。

## 注释约定

遵循全仓 **`comment-standards.mdc`**。Server 侧额外强调：

- 注释**非显而易见**的设计：懒加载/并发、坐标排版算法、plugins 与业务的边界、错误码映射、配置键含义。
- 不写复述代码的注释（如「调用 recognize 方法」）；已有 JSDoc 的工具函数不再重复。
- plugins 类/对外方法用简短 JSDoc；业务 `Service.recognize` 等主流程用步骤注释（`// 1. OCR`）标清编排顺序。

## 鉴权

- 全局 **`JwtAuthGuard`**（`common/guards/auth.guard.ts`），在 `app.module.ts` 注册。
- 取当前用户：**`@GetRequestUser("user")`**；角色元数据：**`@RequireRole()`**。
- 需登录接口加 **`@ApiBearerAuth()`**。

## 控制器与文档

- 使用 `@ApiTags`、`@ApiOperation`。
- DTO 用 **class-validator / class-transformer**；全局 `ValidationPipe`（whitelist、transform），DTO 字段需装饰器才进白名单。
- 统一响应：**`@ApiResult()`** 等装饰器（见 `src/common/decorator`）。
- Swagger：`main.ts` 配置；开发环境可导出 `swagger.json` 供 admin 生成类型。

## 与 admin 协作

- 接口路径与 admin `src/api/**` 的 url 保持一致（如 `/system/user/list`）。
- 复杂查询与事务放在 **Service**，Controller 保持薄层。

## 代码风格

- TypeScript 严格模式；字符串引号与格式以仓库 ESLint/Prettier 为准（当前多为双引号）。

## 示例

```typescript
@ApiTags("systemDept")
@ApiBearerAuth()
@Controller("system/dept")
export class DeptController {
  @ApiOperation({ summary: "部门管理-创建" })
  @ApiResult()
  @Post("/create")
  create(@Body() dto: CreateDeptDto, @GetRequestUser("user") user: RequestUserPayload["user"]) {
    return this.deptService.create(dto, user.userId);
  }
}
```
