---
description: Admin UI 对齐 Ant Design 5.x 价值观、十大原则、布局与全局样式落地约束
globs: apps/admin/**/*.{vue,scss,css,ts}
alwaysApply: false
---

# Ant Design 5.x 设计体系（@apps/admin）

实现栈：**ant-design-vue 4** ≈ Ant Design React **5.x**。列表数据区仍用 **VXE Table**；CS 壳遵守 `--cs-shell-bar-height`。工程/路由/权限见 **`vue-admin.mdc`**；主题见 `theme.scss`、`a-config-provider`。

**CS 顶栏避让**：`html.cs-shell` 下，全屏浮层（Drawer / Modal / Image 预览）与贴顶反馈（message / notification / nprogress）须相对 `--cs-shell-bar-height` 下移或扣高，集中写在 `assets/style/antd.scss`；message/notification 的 inline `top` 由 `App.vue` `syncCsOverlayOffset` 对齐。禁止抬 z-index 盖住 `CsToolsBar`（窗口拖拽/关闭）。

有争议时以官方为准（下列为原则全文入口，规则正文是可执行摘要）：

| 类别 | 文档 |
|------|------|
| 价值观 | https://5x-ant-design.antgroup.com/docs/spec/values-cn |
| 布局 | https://5x-ant-design.antgroup.com/docs/spec/layout-cn |
| 色彩 / 字体 / 图标 / 暗黑 / 阴影 | 官网「全局样式」对应页（规则只收落地约束，不抄色板/字号表） |
| 亲密性 | https://5x-ant-design.antgroup.com/docs/spec/proximity-cn |
| 对齐 | https://5x-ant-design.antgroup.com/docs/spec/alignment-cn |
| 对比 | https://5x-ant-design.antgroup.com/docs/spec/contrast-cn |
| 重复 | https://5x-ant-design.antgroup.com/docs/spec/repetition-cn |
| 直截了当 | https://5x-ant-design.antgroup.com/docs/spec/direct-cn |
| 足不出户 | https://5x-ant-design.antgroup.com/docs/spec/stay-cn |
| 简化交互 | https://5x-ant-design.antgroup.com/docs/spec/lightweight-cn |
| 提供邀请 | https://5x-ant-design.antgroup.com/docs/spec/invitation-cn |
| 巧用过渡 | https://5x-ant-design.antgroup.com/docs/spec/transition-cn |
| 即时反应 | https://5x-ant-design.antgroup.com/docs/spec/reaction-cn |

---

## 价值观（决策滤镜）

改 UI / 交互时用这四条判断「该不该做」：

1. **自然**：视觉与行为符合用户既有认知；少打断、少强迫记忆。
2. **确定性**：克制、模块化、跨页一致；能复用 `FormView` / `ToolButtons` / `feedback` / `card-*` 就不新造一套。
3. **意义感**：每个操作服务任务目标；有结果就要有反馈；勿为装饰分散注意力。
4. **生长性**：新功能可发现，但不靠漫游引导掩盖糟糕信息架构。

5.0 视觉方向（MasterGo / 官方 5.0）：**更聚焦、去干扰、轻松感**——少多层阴影与装饰边框，用留白与字重分层。

---

## 布局与模度

- 网格基数 **8**；布局尺寸尽量 **8 的倍数**。
- 内容区按 **24 栅格** 思路做响应（`a-row`/`a-col` 或等价 Grid）；Gutter 用固定档位（常用 8/16/24），勿随手改。
- 左右布局：侧栏固定、主工作区伸缩（本仓 Sidebar + 内容区已是此模式，勿改成「整页一起挤扁导航」）。
- 区块容器：Uno `card-panel` / `card-section` / `card-meta`；圆角 **8** / 内嵌 **6**（`radius-card` / `radius-inner`）。

---

## 全局样式（落地约束）

官方色板、完整字号阶梯、阴影层级表**不入库**；以下只约束 admin 怎么用。

### 色彩

- **克制**：色服务于信息传递、操作引导、反馈；业务页勿大面积装饰色。
- **品牌色**：唯一来源 `theme.scss` → `--color-primary` + `a-config-provider` token（当前与产品主色对齐）；禁止组件内再写死另一套 Hex。
- **功能色**：成功 / 警告 / 错误 / 信息用 antd 语义（`success` / `warning` / `error` / `processing` 等）或组件默认，勿自造「差不多绿/红」。
- **中性色**：正文、次要文案、边框、分割、布局底用 antd / `--bg-color` / `--border-color` / `--fill-color`；勿页内散落 `#333` `#666` `#f5f5f5` 新魔法值（存量 `reset.scss` 除外，改时收敛到 token）。
- **图表**：ECharts 等可视化可参考 AntV / antd 数据色板，仍优先与主色、功能色体系协调。

### 字体

- 字族、默认 **14px** 正文跟 `reset.scss` + antd；页面**禁止**另起 `font-family` 或私设字号阶梯。
- 层次靠 antd 标题/正文层级或有限字重（400/500/600），勿用花哨展示字体。
- 数字/代码可用现有 mono 栈；金额列表仍配合「右对齐 + 同位小数」。

### 图标

- 统一走 **Iconify**（`IconifyIcon` / `IconSelect` / `useRenderIcon`），图标集固定为 **`ant-design:`**（[Iconify Ant Design](https://icon-sets.iconify.design/ant-design/)）。
- 同场景尺寸一致（工具栏约 16–18、表格内约 14）；语义清晰，勿用图标代替必要文案（尤其危险操作）。
- 菜单 `meta.icon` / 种子数据写 `ant-design:xxx`；禁止再写 `ep:` / `ri:`。
- 同页勿混第三套 icon 体系（不要直接上 `@ant-design/icons-vue` 组件当主路径）。

### 阴影

- 卡片/面板默认**轻或无阴影**，靠边框 + 底色分层（契合 5.0 去干扰）；需要抬升时用 antd 浮层默认（Dropdown / Modal / Popover），勿手写多层 `box-shadow`。
- Uno `card-shadow` 仅用于确需抬升的表面；列表行、侧栏、Tags **不加**装饰阴影。
- 禁止发光描边、彩色 glow 冒充层次。

### 暗黑模式

- **暂不作为一等能力**：未立项前不新增完整 `.dark` 主题覆盖。
- 若启用：必须走 `ConfigProvider` 算法 / CSS 变量与 antd token，并同步 VXE / 壳层；禁止只改几处背景的半套暗黑。

---

## 原则 1 · 亲密性

关联越高越近，越像一个视觉单元。

| 档位 | 值 | 用途 |
|------|-----|------|
| 小 | **8px** | 图标↔文案、标签↔控件、组内紧邻 |
| 中 | **16px** | 同组表单项、工具栏↔表格、卡片内区块 |
| 大 | **24px** | 页边、查询区↔列表、卡片↔卡片 |

- 纵向尽量 `y = 8 + 8n`（`n >= 0`）。Uno 用 `gap-8px` / `16` / `24`；**禁止**随意 13/15/18（现有 shortcut 如 `py-14px` 除外）。
- 三档不够：加减 8 的倍数，或加 **分割线** 拉开层次。
- 组件内部间距 < 组件之间间距。

---

## 原则 2 · 对齐

- **文案**：标题与正文统一左对齐（单一视觉起点）；短字段散落时也要定同一起点。
- **表单**：标签冒号对齐（右对齐标签 + 左对齐控件）优先，提高扫视填写效率；同页勿混用多种标签对齐。
- **数字 / 金额**：同列右对齐、小数位一致；单位放表头，单元格不重复带单位（VXE 列 `align: 'right'` + formatter）。

---

## 原则 3 · 对比

对比必须**强烈**，否则等于没有。

- **主次**：一操作区通常一个 `type="primary"`；可用弱化次项代替再加一个主色。
- **慎重决策**（通过/驳回、不可撤销删除）：系统保持中立，勿用主色诱导；双方次按钮，或危险用 `danger` + 确认。
- **总分**：标题 > 正文 > 辅助，靠字号/字重/颜色，不靠再套重阴影卡片。
- **状态**：静态用 Tag/色点/字典色；动态靠 hover/active 明显区分（可复用 `--dur-press`）。

---

## 原则 4 · 重复

相同元素在全站重复 → 降低学习成本、建立关联。

- 列表页固定模式：**VXE + SearchForm + ToolButtons**（见 `vue-admin.mdc`）。
- 反馈、空状态、危险操作、卡片圆角、间距档位全仓统一；禁止同功能换皮肤（例如有的页 Message、有的页自绘 Toast）。
- 文案格式、状态色、操作列位置保持重复一致。

---

## 原则 5 · 直截了当

「需要在哪里输出，就要允许在哪里输入。」

- 优先上下文编辑：抽屉 / 弹窗 / 行内；勿仅为改单字段整页跳转（现有独立详情路由除外）。
- **行内编辑**：易读优先 → 单击编辑；需强调可编 → 文字链/图标；多字段切换时列宽勿跳动。
- **拖放**（若做）：单维度（仅上下或仅左右）；悬停出手柄；可放置区给主色描边反馈。

---

## 原则 6 · 足不出户

能在本页解决就不要跳页刷新，避免打断心流。

| 模式 | 用法 | Admin 映射 |
|------|------|------------|
| 覆盖层 | 详情 Popover、少量字段输入、不可撤销用 Popconfirm | `a-popover` / `a-popconfirm`；悬停激活约 **0.5s** 延迟，移出立即关 |
| 嵌入层 | 列表展开行、Tabs 分组 | 详情/设置用 Tabs；勿为平级信息连环开页 |
| 流程 | 渐进展现、配置向导 | 复杂流程可用 Modal 内 Steps，仍尽量同页 |

- **删除等可撤销操作**：优先直接执行 + Message「撤销」，勿滥用 Modal 二次确认（狼来了）。
- **不可撤销**：当前页 `Popconfirm` / 明确确认，仍避免无上下文的大 Modal 打断。

---

## 原则 7 · 简化交互（上下文工具）

按费茨法则：目标更大、更近更好点。

- **实时可见**：高频/关键操作常驻（ToolButtons 主操作）。
- **悬停即现**：次要行操作可 hover 再出，避免干扰阅读。
- **开关显示**：点「编辑」才进入输入态（表格编辑模式）。
- **热区**：文字链可点整格（VXE 单元格点击），不必只点短文本；增强响应靠扩大热区而非盲目加大按钮。

---

## 原则 8 · 提供邀请

富交互缺易发现性时，用邀请补示能。

- **静态**：空状态说明下一步、未完成引导、首次功能探索点（须可退出、可重开、文案极短）。
- **动态**：hover 露出可点文字链/按钮；推论邀请（完成 A 后提示相关 B）勿刷屏。
- 漫游引导**不是**烂交互的创可贴；信息架构优先。

---

## 原则 9 · 巧用过渡

动态帮助沟通，不是装饰。

- **保持上下文**：滑入抽屉、折叠面板、页内切换优于整页硬切。
- **解释发生了什么**：增/删/改行可用短暂高亮或过渡；呼出对象（抽屉）与触发源有空间关联。
- **感知性能**：长请求用加载态转移注意，但不能代替真实进度。
- 运动用现有 token：`--ease-out`、`--dur-press` / `--dur-fade` / `--dur-panel`；尊重 `prefers-reduced-motion`。
- **禁止**：侧栏/Tags/大表格整表无意义动效；为动而动。

---

## 原则 10 · 即时反应

交互前有邀请、中有过渡、**后必须有反馈**；输入量级越大反馈越重。

- 统一 `@/utils/feedback`（message / modal / notification / 确认）。
- **查询**：自动完成、实时搜索按类目是否确定选用对应下拉；避免无反馈的静默失败。
- **录入**：提交前实时校验/强度预览优于提交后才报错；提示渐进展现，勿一次堆满。
- **进度**：按钮 loading、表格/列表 loading、整页 loading 按操作量级选用（`v-loading` / `a-spin`）。
- **刷新**：有新内容可「点击刷新」或定时刷新 + 新行短暂高亮。

---

## 全局规则拾要（录入 / 展示）

与原则配合，改表单与列表时遵守：

- **数据录入**：标签面向用户角色（新手通俗 / 专家术语）；敏感信息用暗提示说明原因；提供默认值、结构化格式、placeholder、校验提醒，避免空白猜测。
- **数据展示**：金额数量右对齐；空值有统一占位，勿各页不同。
- 详细全局规则全文：反馈、导航、按钮、数据列表、文案等见官网「设计模式 → 全局规则」目录，本文件不重复粘贴；落地时优先现有组件 API。

---

## 明确禁止

- 散落魔法间距、页内私设品牌色、多套反馈皮肤。
- 用 MasterGo 逐像素标注覆盖 antd 组件默认尺寸。
- 滥用 Modal 确认、整页跳转完成本可页内完成的事。
- 用本规范否定 VXE / CS 壳既有约束。

---

## 改 UI 自检

1. 价值观：是否克制、可复用、服务任务目标？
2. 模度：间距/尺寸是否 8n？常用是否 8/16/24？
3. 色彩/字体/图标/阴影是否走 token 与现有入口，无魔法值与多层阴影？
4. 亲密性 / 对齐 / 对比 / 重复是否成立？
5. 交互：能否足不出户？工具是否按重要性可见？有无邀请与即时反馈？
6. 过渡是否解释变化、是否尊重 reduced-motion？
7. 是否仍符合 `vue-admin.mdc` 列表页模式？
